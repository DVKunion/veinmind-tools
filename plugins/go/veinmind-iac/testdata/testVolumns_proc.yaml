apiVersion: v1
kind: Pod
metadata:
  name: hello-docker-socket
spec:
  containers:
    - command:
        - sh
        - "-c"
        - echo 'Hello' && sleep 1h
      image: busybox
      name: hello
  volumes:
    - name: 444test-volume
      hostPath:
        path: /proc
        type: Directory
